error: unknown stage 'tiled'

Usage: ./bin/perf_harness <stage> [H W K] [seed] [--check] [--iters=N]
  stage: naive | reorder | unroll | tile | simd | optimized
  H W K default to 2048 2048 3; seed defaults to 1234.
  --check   : also run conv_naive once and report max abs diff.
              Do NOT use --check while perf is attached.
  --iters=N : run the stage N times in this one process
              (default 10). Divide perf's totals by N for
              per-run averages -- see Makefile's mpki target.
